export const setActiveli = (activeli) => {
    if(activeli == 0)
        return "Add Customer"
    else if(activeli == 1)
        return "Add Project"
    else if(activeli == 2)
        return "Add Employee"
}